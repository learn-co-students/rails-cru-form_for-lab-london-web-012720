<h1><%=@song.name%></h1>

<%= link_to 'Edit', edit_song_path(@song) %> 
<%= link_to 'Back', songs_path %> 

<h1><%= link_to 'My Genre', genre_path(@song.genre) %> </h1>
<%= link_to "My Artist", artist_path(@song.artist) %> 
