<h1><%= @song.name %></h1>

<p> Artist Name: <%= (link_to @song.artist.name, artist_path(@song.artist)) if @song.artist %><br /> </p>
Song Genre: <%= (link_to @song.genre.name, genre_path(@song.genre)) if @song.genre %>